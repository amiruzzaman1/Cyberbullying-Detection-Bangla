name: Cyberbullying Detection Cron Job

on:
  schedule:
    - cron: '*/5 * * * *'  # Run every 5 minutes
  workflow_dispatch:

jobs:
  detect-cyberbullying:
    runs-on: ubuntu-latest

    steps:
    - name: Visit website
      run: curl https://cbarabic.streamlit.app/
